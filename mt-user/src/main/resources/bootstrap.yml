spring:
  cloud:
    # 使用 Nacos 作为配置中心
    nacos:
      config:
        group: DEFAULT_GROUP
        server-addr: 39.108.187.54:8848
        file-extension: yml
        prefix: api
  profiles:
    active: prod
