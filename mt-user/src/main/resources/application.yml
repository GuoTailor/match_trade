server:
  port: 84

spring:
  application:
    name: api
  cloud:
    zookeeper:
      connect-string: localhost:2181
      max-retries: 3
  redis:
    host: localhost
    port: 6379
    timeout: 5000
  r2dbc:
    username: postgres
    password: admin
    url: r2dbc:postgres://localhost:5432/match_trade
    pool:
      enabled: true

logging:
  level:
    org:
      springframework:
        data:
          r2dbc: DEBUG
